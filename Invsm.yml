# inventory.yml

all:
  children:
    # Combined switches group
    switches:
      hosts:
        10.10.1.100:  # MGMT_switch
        10.10.1.101:  # User_switch
        10.10.1.102:  # ACCT_switch
        10.10.1.103:  # IT_seitch
        10.10.1.104:  # Local_switch
      vars:
        ansible_user: admin
        ansible_password: ""  # No password required
        ansible_connection: ansible.netcommon.network_cli
        ansible_network_os: exos

    # Windows desktops grouped by network role
    windows_desktops:
      hosts:
        10.10.1.105:  # WindowsDesktop-1
        10.10.1.106:  # WindowsDesktop-2
        10.10.1.107:  # WindowsDesktop-3
        10.10.1.108:  # WindowsDesktop-4
      vars:
        ansible_user: Administrator
        ansible_password: P@ssw0rd
        ansible_connection: winrm
        ansible_port: 5985

    # Linux test boxes
    linux_test_boxes:
      hosts:
        10.10.1.109:  # Test_Box_1
        10.10.1.110:  # Test_Box_2
      vars:
        ansible_user: student
        ansible_password: P@ssw0rd
        ansible_become: true
        ansible_become_method: sudo
        ansible_become_password: P@ssw0rd

